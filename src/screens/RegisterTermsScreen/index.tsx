import React, {FunctionComponent} from 'react';
import {View, Text} from 'react-native';

interface Props {}

const RegisterTermsScreen: FunctionComponent<Props> =
  function RegisterTermsScreen() {
    return (
      <View>
        <Text>RegisterTermsScreen</Text>
      </View>
    );
  };
export default RegisterTermsScreen;
