import {useNavigation} from '@react-navigation/native';
import {RootStackNavigationProps} from '@src/navigations/RootStackNavigator/types';

export default () => useNavigation<RootStackNavigationProps>();
